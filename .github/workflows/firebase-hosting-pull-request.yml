# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
'on': pull_request
jobs:
  create-envfile:
 
    runs-on: ubuntu-18.04
 
    steps:
    - name: Make envfile
      uses: SpicyPizza/create-envfile@v1
      with:
        VUE_APP_IEX_API_TOKEN: ${{ secrets.VUE_APP_IEX_API_TOKEN }}
        VUE_APP_IEX_BASE_URL: ${{ secrets.VUE_APP_IEX_BASE_URL }}
        VUE_APP_API_KEY: ${{ secrets.VUE_APP_API_KEY }}
        VUE_APP_AUTH_DOMAIN: ${{ secrets.VUE_APP_AUTH_DOMAIN }}
        VUE_APP_DATABASE_URL: ${{ secrets.VUE_APP_DATABASE_URL }}
        VUE_APP_PROJECT_ID: ${{ secrets.VUE_APP_PROJECT_ID }}
        VUE_APP_STORAGE_BUCKET: ${{ secrets.VUE_APP_STORAGE_BUCKET }}
        VUE_APP_MESSAGIN_ID: ${{ secrets.VUE_APP_MESSAGIN_ID }}
        VUE_APP_APP_ID: ${{ secrets.VUE_APP_APP_ID }}
        VUE_APP_MEASUREMENT_ID: ${{ secrets.VUE_APP_MEASUREMENT_ID }}
        file_name: .env
jobs:
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_DIVIDENDS_INFO }}'
          projectId: dividends-info
